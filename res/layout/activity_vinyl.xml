<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/VinylActivityScrollLayout"
    style="@style/MainLayout">

    <LinearLayout
        android:id="@+id/VinylActivityLinearLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        tools:context=".VinylActivity" >

        <ImageView
            android:id="@+id/VinylActivitySpinner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_launcher" 
            android:contentDescription="loading image"/>

        <Button
            android:id="@+id/VinylActivityButtonTakeImage"
            style="@style/Button"
            android:onClick="takeImage"
            android:text="Foto aufnehmen" />

    </LinearLayout>

</ScrollView>